

# Add Sign In Route

## Changes

1. **Create `src/routes/sign-in.tsx`** — A dedicated sign-in page with email/password fields, a "Sign In" button, and links to "Forgot Password" and "Sign Up". Styled consistently with the brand system. Static for now (no auth backend).

2. **Update `src/components/layout/Header.tsx`** — Change the "Sign In" link from `/request-demo` to `/sign-in`.

