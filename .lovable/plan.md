## Replace logo asset

Overwrite `src/assets/SapienceHCMLogo.png` with the uploaded `Sapience_HCM_Vertical-2.png`.

### Steps
1. Copy `user-uploads://Sapience_HCM_Vertical-2.png` to `src/assets/SapienceHCMLogo.png` (overwrite).

### Notes
- No code changes needed — Header, Footer, and Sign-in already import from this path.
- Existing sizing (`h-12` header, `h-16` footer/sign-in) should still work since the new image is also vertical/stacked.
